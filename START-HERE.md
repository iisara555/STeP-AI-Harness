# เริ่มที่นี่ — STeP AI

ไฟล์นี้เป็นใบเริ่มต้นสั้นสำหรับ First Run และตัวติดตั้ง หากต้องการคู่มือพนักงานฉบับเต็ม ให้เปิด [README.md](README.md)

## ใช้งานแบบย่อ

1. รับไฟล์ชุดติดตั้งจาก Shared Drive หรือช่องทางภายในที่ AI Champion แจ้ง
2. แตกไฟล์ ZIP
3. เปิด `Install-STeP-AI.bat` (Windows) หรือ `Install-STeP-AI.command` (macOS)
4. เลือกทีมหลักจาก **22 ทีม**; `0` หรือกด Enter = **ยังไม่แน่ใจ — ข้ามก่อนและเลือกภายหลังได้**
5. เปิดโฟลเดอร์นี้ด้วยโปรแกรม AI ที่องค์กรอนุมัติ — ตัวติดตั้งเตรียม instruction สำหรับโปรแกรมที่รองรับไว้ให้อัตโนมัติ ไม่ต้องเลือกค่ายตอนติดตั้ง
6. พิมพ์ `เริ่มใช้งาน STeP AI` หรือเริ่มงานจริงเป็นภาษาไทย

ถ้าเครื่องยังไม่มีโปรแกรม AI สามารถเริ่มด้วย **Cursor** หรือ **OpenCode** ได้ ไม่ต้องติดตั้ง Node.js, Git หรือแก้ระบบเอง

## First Run

เมื่อพิมพ์ `เริ่มใช้งาน STeP AI` ถ้าคุณเลือกทีมไว้แล้ว ระบบจะแสดงตัวอย่างงานเริ่มต้นของทีมนั้น 3 แบบ หากยังไม่ได้เลือกทีม ให้เริ่มงานจริงได้ทันที ระบบจะช่วยงานก่อน แล้วค่อยเสนอทีมที่น่าจะเกี่ยวข้องให้คุณยืนยันภายหลัง

ใน First Run ให้ใช้เฉพาะไฟล์เริ่มต้นและข้อมูลผู้ใช้ที่จำเป็นก่อน ห้ามไล่อ่าน Skills, Rules หรือ Manifest ทั้งหมดจนกว่าจะมีงานจริงที่ต้อง route

## หลังติดตั้ง

- เปลี่ยนทีมภายหลัง: `step-ai config`
- อัปเดต: `Update-STeP-AI.bat` หรือ `Update-STeP-AI.command`
- แจ้งปัญหา: `Feedback-STeP-AI.bat` หรือ `Feedback-STeP-AI.command`
- คู่มือเต็ม: [docs/employee-guide.md](docs/employee-guide.md)
