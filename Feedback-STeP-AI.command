#!/usr/bin/env bash
# STeP AI — Feedback & Skill Contribution (macOS Launcher)
# อุทยานวิทยาศาสตร์และเทคโนโลยี มหาวิทยาลัยเชียงใหม่ (STeP)

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
bash "$SCRIPT_DIR/install/feedback-macos.sh"
