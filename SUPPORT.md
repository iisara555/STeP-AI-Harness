# STeP AI Support

**สถานะ:** ใช้งานได้ตั้งแต่วันแรกของ Pilot

**ผู้ดูแลกลาง:** ยังไม่ได้แต่งตั้ง

**ผู้ประสานงาน Pilot วันที่ 22 กันยายน 2569:** STeP AI Geek

**การแจ้งปัญหาระหว่าง Pilot:** ติดต่อ STeP AI Geek ผ่านช่องทางภายในที่ใช้อยู่ โดย STeP AI Geek แจ้งช่องทางให้ผู้ทดสอบทุกคนทราบก่อนเริ่ม; ไม่มีการตั้งห้องแชทหรืออีเมลใหม่อัตโนมัติ

**ช่องทางชั่วคราวที่ใช้ตอนนี้:** **หัวหน้าทีมหรือผู้ประสานงานทีม ผ่านช่องทางภายในที่ทีมใช้อยู่ตามปกติ**

เอกสารนี้เป็น **จุดอ้างอิงกลางเพียงจุดเดียว** สำหรับการขอความช่วยเหลือ STeP AI ในช่วงที่ยังไม่มีผู้รับผิดชอบกลางอย่างเป็นทางการ

## macOS บล็อกไฟล์ติดตั้ง ตรวจสอบผู้พัฒนาไม่ได้

เป็นอาการที่พบบ่อยที่สุดของผู้ใช้ Mac และไม่ใช่ความผิดพลาดของชุดติดตั้ง

ชุดติดตั้งยังไม่ได้ผ่านการ notarize กับ Apple ไฟล์ทุกไฟล์ที่แตกจาก ZIP ที่โหลดผ่านเบราว์เซอร์
จะถูกติดแฟล็ก quarantine และ macOS จะบล็อกสคริปต์ที่ไม่ได้ลงนามตั้งแต่ครั้งแรก

วิธีเปิดครั้งแรก:

1. ดับเบิลคลิก `Install-STeP-AI.command` ให้มันถูกบล็อกหนึ่งครั้งก่อน
2. ไปที่ **System Settings > Privacy & Security** แล้วเลื่อนลงล่างสุด
3. กด **Open Anyway** ที่บรรทัดของไฟล์นั้น แล้วยืนยันด้วยรหัสผ่านเครื่อง

ปุ่ม Open Anyway จะปรากฏก็ต่อเมื่อเพิ่งถูกบล็อกไปแล้วเท่านั้น ถ้าไม่เห็นให้ดับเบิลคลิกไฟล์ใหม่ก่อน

ถ้าเปิด Terminal ได้ ใช้ทางลัดนี้แทนได้ โดยเปลี่ยน path ให้ตรงกับที่แตกไฟล์ไว้:

```text
xattr -dr com.apple.quarantine ~/Downloads/STeP-AI-Pilot-v0.7.4
```

คำสั่งนี้ลบเฉพาะแฟล็ก quarantine ของไฟล์ในโฟลเดอร์ชุดติดตั้ง ไม่ได้ติดตั้งอะไรเพิ่ม
และไม่แตะไฟล์นอกโฟลเดอร์นั้น

**บน macOS 14 หรือเก่ากว่า** ยังใช้วิธีคลิกขวาที่ไฟล์แล้วเลือก Open ได้
Apple ถอดวิธีนี้ออกตั้งแต่ macOS 15 (Sequoia) จึงต้องใช้ System Settings แทน

ทำครั้งเดียวพอ หลังอนุญาตครั้งแรก ตัวติดตั้งจะล้างแฟล็กให้ไฟล์ที่เหลือในชุดเอง
จึงไม่ต้องทำซ้ำกับ Update / Feedback / Check-Privacy

รายละเอียดขั้นตอนอยู่ใน `MAC-START-HERE.txt` ที่มาพร้อมชุดติดตั้ง

## ถ้าติดตั้งหรืออัปเดตไม่สำเร็จ

ส่งข้อมูลต่อไปนี้ให้หัวหน้าทีมหรือผู้ประสานงานทีมของคุณผ่านช่องทางภายในที่ทีมใช้อยู่:

1. ภาพหน้าจอข้อความผิดพลาด
2. ระบบปฏิบัติการ: Windows หรือ macOS
3. รุ่น STeP AI ที่กำลังติดตั้ง เช่น `v0.7.4`
4. ขั้นตอนที่ทำก่อนเกิดปัญหา 1–2 บรรทัด
5. ถ้าระบบเปิดได้ ให้แนบผลจาก `step-ai doctor --employee`

**ไม่ต้องติดตั้ง Node.js, Git หรือแก้ไฟล์ระบบเองเพื่อแก้ปัญหา**

## ถ้า AI ตอบไม่ถูก

ใช้:

- `Feedback-STeP-AI.bat` บน Windows
- `Feedback-STeP-AI.command` บน macOS

หรือรัน:

```text
step-ai feedback --issue
```

กรอก `FEEDBACK.md` แล้วส่งผ่านช่องทางภายในของทีมให้ **หัวหน้าทีมหรือผู้ประสานงานทีม** เพื่อรวบรวมส่งต่อเมื่อกำหนดผู้ดูแลกลางแล้ว

## ถ้าอยากให้ STeP AI ช่วยงานใหม่

รัน:

```text
step-ai feedback --request
```

กรอก `REQUEST_NEW_TASK.md` แล้วส่งผ่านช่องทางภายในของทีมให้ **หัวหน้าทีมหรือผู้ประสานงานทีม**

## ข้อมูลที่ห้ามแนบในคำขอช่วยเหลือ

ห้ามส่ง:

- password, token, cookie, MFA, API key หรือ secret
- screenshot ที่เห็น credential หรือ session
- เอกสารลูกค้า/ผู้ประกอบการฉบับเต็มถ้าไม่จำเป็น
- PII หรือข้อมูลภายในที่ไม่เกี่ยวกับปัญหา

ถ้าต้องส่งภาพหน้าจอ ให้ crop หรือปิดบังข้อมูลที่ไม่จำเป็นก่อน

## Public GitHub

Repository ปัจจุบันเป็น Public จึง **ไม่ใช้ Public GitHub Issue เป็นช่องทาง support สำหรับพนักงาน** โดยเฉพาะกรณีที่มี screenshot, เอกสารภายใน หรือข้อมูลลูกค้า

## เมื่อมีผู้ดูแลกลางในอนาคต

เมื่อ STeP กำหนด support owner หรือห้องแชทกลางแล้ว ให้แก้เฉพาะส่วน **ช่องทางช่วยเหลือปัจจุบัน** ในไฟล์นี้ และให้ README / Employee Guide / installer อ้างอิงมาที่ `SUPPORT.md` ต่อไป ไม่กระจายชื่อบุคคลหรือช่องทางซ้ำหลายไฟล์
